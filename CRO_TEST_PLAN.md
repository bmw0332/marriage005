# CRO_TEST_PLAN｜無料個別診断LP 改善計画

公開後、どの数値を見て、何を、どの順番で変えるかの計画です。

---

## 1. 最重要KPI

**予約完了率 ＝ 無料個別診断の予約完了数 ÷ 有効なLPセッション数**

初期目標：**100セッションあたり3〜5件の予約完了**

- この目標値はサイト内に一切掲載していません
- 保証を意味する表現も使用していません
- 「有効なセッション」＝ボット・自社アクセスを除いたセッション。GA4の内部トラフィック除外を必ず設定してください

### 判断に必要な最低ボリューム

| 用途 | 目安 |
|---|---|
| ファーストビューの良し悪しを判断 | 300セッション以上 |
| 予約完了率の差を判断 | 各案500セッション以上、または予約完了20件以上 |

**100セッション程度で「効果があった／なかった」と判断しないでください。** 予約完了率3〜5％では、1〜2件の差は誤差の範囲です。

---

## 2. ファネルと計測イベント

| 段階 | イベント | 健全な目安 |
|---|---|---|
| 1. LP表示 | `lp_view` | 100% |
| 2. CTAクリック | `lp_cta_click` | 15〜30% |
| 3. 予約セクション到達 | `booking_section_view` | CTAクリックの85%以上 |
| 4. ウィジェット表示 | `timerex_widget_loaded` | 到達の95%以上（埋め込み設定後） |
| 5. 日程選択・入力開始 | `booking_start` | 到達の40〜60% |
| 6. 予約完了 | `booking_complete` | 開始の50〜70% |
| 7. サンクス表示 | `thank_you_view` | 完了とほぼ同数 |

目安は初期の仮置きです。2週間の実測値が出たら、この表を実データで更新してください。

**CTAクリックを予約完了として扱わないでください。** 広告の最適化対象は `booking_complete` のみです。

---

## 3. ヘッドライン候補（A/Bテスト用）

**現在の公開中はD案です（2026-09-06〜）。**

### D案：解決提示型（現在の実装）

```
婚活、次は何を変えればいい？
その答えを、プロと見つける60分。
```

- 問題提起と解決提示を1つの見出しに収めている
- 「プロ」「60分」で、誰が・どれだけの時間で答えを出すかが分かる
- **広告クリエイティブの文言も、この表現へ合わせてください。** 一致していないとCTAクリック率が落ちます

以下は当初の候補です。D案の実測値が出たあとの比較対象として残しています。

### A案：問題認識型

```
今の婚活、
どこで止まっていますか？
```

- 広告の訴求と最も一致する
- 「原因が分からない」層に刺さる
- 対象を絞りすぎない

### B案：成果型

```
今の婚活で最初に変える1〜2点を、
60分で明確にします
```

- 持ち帰れるものが明確
- 「60分使って何も残らない」不安への先回り
- 一方で、問題認識が浅い層には自分ごとになりにくい

### C案：原因不明型

```
なぜ続かない婚活を、
次に試す行動へ
```

- 「原因が分からない」に最も直接的
- 対象が絞られるぶん、母数は落ちる可能性がある

### 差し替え方法

`index.html` の `<h1>` と、直下の `.hero__lede` を書き換えます。
同時に、`assets/js/analytics.js` の `DEFAULT_VARIANT` を `'A'` / `'B'` / `'C'` / `'D'` に変更してください。
全イベントに `lp_variant` が付くため、GA4で案別の予約完了率を比較できます。

**最初から複数ページへアクセスを自動分割しないでください。** まずD案を一本で運用し、十分なデータを取ってから比較に進みます。

---

## 4. 公開後の診断ロジック

数値の見え方から、次に確認する箇所を特定します。

### 4-1. LPアクセスはあるが、CTAクリックが少ない（15%未満）

問題は**ファーストビュー**にあります。

- 広告クリエイティブとH1の表現が一致しているか
- 広告の訴求と、LPが約束していることがずれていないか
- ファーストビュー内にCTAが見えているか（375px幅で実機確認）
- CTA文言が行動を示しているか
- 無料であること、60分であること、診断だけで終われることが読めているか
- リード文が長すぎないか

**まず疑うのは広告とのメッセージ不一致です。** LPだけを見て判断しないでください。

### 4-2. CTAクリックはあるが、予約セクションへ到達しない

`lp_cta_click` に対して `booking_section_view` が85%を大きく下回る場合。

- スクロールが正しく `#booking` へ止まっているか（固定ヘッダーのぶんズレていないか）
- 途中で外部サイトへ遷移していないか
- JavaScriptエラーが出ていないか（実機のブラウザコンソール）
- ボタンのタップ判定が効いているか
- ページの読み込みが遅く、離脱していないか

### 4-3. 予約セクションは見られているが、日程選択されない

`booking_section_view` に対して `booking_start` が40%を下回る場合。

- **空き枠が十分にあるか**（平日夜・土日が埋まっていないか）。最も多い原因です
- 直近の予約可能日が遠すぎないか（1週間先しか空いていないと離脱します）
- ウィジェットが表示されているか、表示が遅くないか
- 60分という所要時間が負担に見えていないか
- 「診断だけで終了できる」がこの位置で読めているか

### 4-4. 日程選択はされるが、予約完了しない

`booking_start` に対して `booking_complete` が50%を下回る場合。

- **入力項目が多すぎないか**（推奨は氏名・メールの2項目のみ）
- 電話番号・住所などを必須にしていないか
- 入力エラーが分かりにくくないか
- スマートフォンでの操作性（入力欄のサイズ、キーボードの種類）
- 確認画面が多段階になっていないか

README第6章の推奨設定を適用済みか確認してください。

### 4-5. 予約完了するが、面談へ来ない

- リマインドメールが送られているか（前日・当日）
- 当日のURLが分かりやすい位置にあるか
- 予約から面談までの日数が空きすぎていないか（3日以内が理想）
- 予約完了メールが迷惑メールに入っていないか

### 4-6. 面談には来るが、対象者ではない

- 広告のターゲティング設定
- LPの「一つでも当てはまるなら、相談対象です」の項目が広すぎないか
- 広告クリエイティブが対象を示しているか
- TimeRexの任意項目「現在、最も困っていること」の回答傾向を確認

---

## 5. テストの進め方

### 原則

**ページ全体を一度に変更しないでください。**
一つの問題に対して、一つの要素だけを変更します。何が効いたか分からなくなると、次の判断ができません。

### 優先順位

上流から順に直します。ファーストビューが弱いまま予約フォームを整えても、母数が増えません。

1. ファーストビュー（H1・リード文・CTA文言・CTAの位置）
2. 予約セクション（空き枠・入力項目）
3. 対象者確認セクション（項目の表現）
4. 実績・担当者（信頼の伝わり方）
5. 不安解消セクション（表現の具体性）
6. FAQ（順番・追加削除）

### 変更履歴の記録

| 日付 | 変更箇所 | 変更前 | 変更後 | 期間 | セッション数 | 予約完了数 | 予約完了率 |
|---|---|---|---|---|---|---|---|
| | | | | | | | |

この表を埋め続けてください。記録がないと、同じ変更を繰り返すことになります。

### テスト候補（優先度順）

| # | 対象 | 内容 | 想定する効果 |
|---|---|---|---|
| 1 | H1 | D案 → A案（広告と完全一致させた状態） | 成果の明確さで、行動意欲の高い層を拾う |
| 2 | 主CTA文言 | 「無料で空き日程を確認する」→「空いている日時を見る」 | 心理的負担をさらに下げる |
| 3 | 実績の位置 | 実績セクションをファーストビュー直下へ | 信頼を先に置いたときの効果 |
| 4 | 診断イメージ | ファーストビューでの表示位置（スマートフォンでCTAより上／下） | 具体性の提示タイミング |
| 5 | 対象者確認 | 7項目 → 5項目 | 読む負担の軽減 |
| 6 | 不安解消 | 予約セクションの直前 → ファーストビュー直下にも要約を追加 | 営業不安の早期解消 |
| 7 | 事例 | 2件 → 1件 | ページ短縮と離脱率の関係 |

### 1回のテスト期間

最低2週間、または各案500セッション以上。曜日による差が大きいため、**1週間未満での判断は避けてください。**

---

## 6. 定点観測する項目

週次で確認します。

- セッション数（`utm_source` 別）
- 予約完了率
- ファネル各段階の通過率
- `cta_position` 別のクリック数（どの位置のCTAが効いているか）
- `faq_open` の質問別開封数（不安が集中している論点が分かります）
- スマートフォン／PC別の予約完了率
- 予約完了 → 面談実施の率

`faq_open` の傾向は、次にファーストビューへ引き上げるべき情報を教えてくれます。
特定の質問が突出して開かれている場合、それは**FAQに置くべきではない情報**です。
